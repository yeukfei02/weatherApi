const weatherController = require('../controller/weather');

describe('main.test', () => {
  describe('weather', () => {
    test('getWeather', () => {
      expect(123).toBeDefined();
    });
  });
});
